// layoutHelpers.js
// Utility functions for Layout logic

// Returns true if the navigation should be hidden for the current page
export function shouldHideNavigation(pathname) {
  return pathname === '/launcher';
}

// Add more helpers as needed...
